using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GhostFading : MonoBehaviour {
	/*
	public float VisibilityDuration = 3f;
	
	SkinnedMeshRenderer mr;
	Material[] mats;
	Color color;
	float elapsed = 0f;
	
	void Start() {
		mr = gameObject.transform.GetChild(0).GetComponent<SkinnedMeshRenderer>();
	}
	
	void Update() {
		mats = mr.materials;
		color = mats[0].color;
		color.a = Mathf.Cos(elapsed * 360);
		mats[0].color = color;
		mr.materials = mats;
		
		elapsed += Time.deltaTime;
	}
	*/
}
